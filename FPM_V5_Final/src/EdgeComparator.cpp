#include <iostream>
#include "EdgeComparator.h"

using namespace std;

/*
bool EdgeComparator::compatible(void *pa, void *pb)
{
  //cout << "ptr1: " << pa << " ptr2: " << pb << endl;
  //cout<<"come in tne comparator."<<endl;
  int *a=(int *)pa;
  int *b=(int *)pb;
 // cout << "a: " << *a << " b: " << *b << endl;
  
  int c;
  c=*a-*b;
  if(c<=floatNum&&c>=-floatNum)
  {  //cout<<*a<<"and"<<*b<<"is matched"<<endl;
     return true;
  }
  else
  {  //cout<<*a<<"and"<<*b<<"is not matched"<<endl;
     return false;
  }
}
*/
